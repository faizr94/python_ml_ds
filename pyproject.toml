[project]
name = "python-ml-ds"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "nbstripout>=0.9.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]

[project.scripts]
python-ml-ds = "python_ml_ds:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
